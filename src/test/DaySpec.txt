package aoc.y%YEAR%.day%DAY%

import aoc.InputReader
import spock.lang.Specification
import spock.lang.Unroll

@Unroll
class Day%DAY%Spec extends Specification {

    def "part1 warmup tests"() {
        expect:
        Day%DAY%.part1(input) == output

        where:
        input  | output
    }

    def "part1"() {
        expect:
        Day%DAY%.part1(InputReader.read("y%YEAR%/day%DAY%")) == "this will be the answer"
    }

    def "part2 warmup tests"() {
        expect:
        Day%DAY%.part2(input) == output

        where:
        input  | output
    }

    def "part2"() {
        expect:
        Day%DAY%.part2(InputReader.read("y%YEAR%/day%DAY%")) == "this will be the answer"
    }
}
